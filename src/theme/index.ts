import { createMuiTheme } from "@material-ui/core"

export const darkTheme = createMuiTheme({
  palette: {
    background: {
      default: '#161616'
    }
  },
  typography: {
    allVariants: {
      color: "#fff"
    }
  }
})